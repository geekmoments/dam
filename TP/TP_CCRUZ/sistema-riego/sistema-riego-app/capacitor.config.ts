import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fiuba.ceiot.dam.com',
  appName: 'TP_SR_APP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
