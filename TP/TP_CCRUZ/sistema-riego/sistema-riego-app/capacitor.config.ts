import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sistema-riego-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
